//! Provider adapter boundary. Concrete providers are added in later milestones.

pub use pi_model::{ModelEventStream, ModelRequest, ModelService, ModelServiceError};
pub use pi_protocol::ProviderId;
